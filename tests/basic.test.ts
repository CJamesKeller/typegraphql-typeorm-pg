import "reflect-metadata";

// set up Docker w/ test db, seed it, validate mutation, validate query, validate error scenarios

describe("basic testing", () => {
    it("performs as expected", () => {
        expect(true).toBeTruthy();
    });
});
